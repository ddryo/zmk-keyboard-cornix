## ファームウェア上書き手順

左右同じ手順。（左からのほうがよさそうかも）

1. UF2 ブートローダに入れる（リセットボタンを素早く 2 回）。
2. 初回書き込み時や、config などに変更があった時は、`reset_nosd.uf2`を書き込み、設定を初期化。（キーマップ変更だけならこれはスキップ）
3. 左は`cornix_left_default_nosd.uf2` / 右には `cornix_right_nosd.uf2`を書き込む。
